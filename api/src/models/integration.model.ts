// static method to get all providers (getProviders())
// get(name, teamId)
// save(name, teamId)

/**
 * Interact with the integrations database
 * @export
 * @class IntegrationsModel
 */
export class IntegrationsModel {

}
